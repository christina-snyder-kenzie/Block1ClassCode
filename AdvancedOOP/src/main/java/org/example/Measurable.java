package org.example;

public interface Measurable {
    //methods are automatically public and abstract
    //this means we write method signatures like this:
    //returnType methodName(parameters);
    double getMeasure();
}
